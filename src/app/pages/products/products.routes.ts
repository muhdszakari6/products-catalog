import { Routes, } from '@angular/router';
import { ProductsComponent } from './products.component';

export const redeemFlowRoutes: Routes = [
  {
    path: '', component: ProductsComponent
  },
];

